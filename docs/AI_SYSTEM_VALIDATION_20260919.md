# AI 系统工具与图标验证 — 2026-09-19

## 交付内容

- 原 6 个时间/提醒 MCP 工具扩展为 21 个，覆盖本机状态、音量、12 个页面入口、指定日期、天气数据、专注计时、笔记和录音控制。
- 异步操作有 ID 和结果查询，录音等待小智回答结束；超时、取消、忙碌和保存失败不会报告成功。
- AI 笔记加入应用目录及分页阅读。两份 SD 快照轮换并带 CRC，不占用仅 16 KB 的共享 NVS。
- Lucide 原始 SVG、固定版本、完整许可和生成脚本纳入项目。主页、目录、箭头、电量和充电图标更新；按数字与图标的实际像素高度居中。
- USB 诊断输出改为带缓冲的驱动及实际发送字节数重试。此前 IDF 的无驱动 VFS 发送会在超时丢字节后仍返回原长度；实测出现 `succeeded` 缺成 `succeded`、JSON 断字和截图 HEX 不完整。新增发送回归测试并在实机复核。

## 主机验证

- ESP-IDF 6.0.1 完整构建成功。
- `check_system_tools.py`：21 工具/7 页、严格字段/数字检查、通知不写入、重试不重复创建、笔记增改查删/容量/重启/失败回滚、专注到期、队列容量/取消/过期；SD 新快照截断时恢复旧快照，双份损坏时拒绝覆盖。
- `check_reminders_contract.py`：原 6 工具兼容及提醒持久化/到期/稍后语义通过。
- `check_serial_write.py`：实际发送函数在短写/暂时写不入时保留全部字节，永久阻塞在约 2 秒内失败，退出释放输出锁。
- `check_ui_contract.py`：7 行目录及首页几何通过。
- `check_ai_contract.py`、`check_dashboard_contract.py`、`check_input_transport.py`、`check_volume_callbacks.py`、`check_refresh_policy.py`、`check_reminder_chime.py`、`check_recorder.py` 通过。
- 实际 C++ 绘图函数渲染 5 类场景，共 295 帧，缺字与文本越界检查通过。包括 100% 充电、0/25/50/100% 电量、长笔记分页、空列表、闰月等。
- 针对 MCP/队列/录音/存储做独立只读审查，已核实入口互斥锁和取消顺序，未发现剩余具体阻断问题。图标生成用的 resvg 依赖在官方 npm 注册表 audit 为 0 漏洞；不进入固件运行时。

## 烧录与设备验证

端口 `/dev/cu.usbmodem21101`。实际读取并核对分区表，仅刷应用 `0x80000`，未刷 NVS、分区表、SD 或字体分区。esptool 返回 `Hash of data verified`。

- 镜像：`build/metalio-hw-test.bin`
- 大小：3,033,776 字节
- SHA-256：`b8c5dbd4791bd49986bf2b783af1cdd5b57a62857c46accc57319b66436aa3e9`

实机结果：

1. 诊断 MCP 完整发现 21 个工具；小智服务端也完成 `initialize=1 tools_list=7`，工具可从云端发现。
2. 音量设置、读取、拒绝 101 越界值并恢复原值通过。
3. 12 个应用入口逐个打开，读取实际页面匹配；闰日 `2028-02-29` 显示正确，非法日期 `2026-02-29` 被拒绝。
4. 笔记创建、读取、修改、目录、正文及下一页显示通过。硬件复位后正文完整恢复，随后删除本轮临时笔记，笔记目录恢复测试前状态。
5. 一分钟专注提醒创建并查询成功，测试后仅删除新建提醒。
6. 最近录音从 SD 恢复，保持原有 5 秒片段；取消排队的录音没有覆盖它，MCP 回放完成且无失败状态。本轮未用新录音覆盖用户现有片段。
7. 数据查询返回真实有效性和更新时间，后台刷新返回 `requested`。
8. 截图传输 CRC 全部通过，字库 `ready=1 glyphs=45248`。
9. 应用目录的 AI 笔记触摸入口、第 7 项阅读的物理按键路径通过；修复后连续 100 条 MCP 状态回复均完整可解析。重启后云端完成两轮完整发现（`initialize=2 tools_list=14`），连接 `ws=1 sess=1`。
10. 原提醒 ID `[1,3,17]` 的完整列表与测试前相同；音量已恢复，测试数据清理后回到首页。

## 证据与边界

[设备首页](ui/qa/system-home-final-20260919.png)、[应用目录](ui/qa/system-apps-20260919.png)、[笔记目录](ui/qa/system-notes-20260919.png)、[笔记第二页](ui/qa/system-note-page-2-20260919.png)、[重启恢复](ui/qa/system-note-restored-20260919.png)、[闰年日历](ui/qa/system-calendar-leap-20260919.png)。

截图来自设备真实 framebuffer 的 CRC 校验读回，属于像素内容证据，不是光学拍照或肉眼闪烁评价。本轮自动化覆盖工具调用与云端发现，尚未用人工语音确认每条自然语言示例。示例和当前能力边界见 [AI 系统工具](AI_SYSTEM_MCP.md)。
