# 语音工作记忆 A1 验证 — 2026-09-21

基于 `codex/ai-reader-home` 的 `e4c3a7822ce8ec9fd941e24b2b478f625f6eef26`。

## 执行环境与证据

Linux x86_64 主机，C++17；实际编译仓库的 notes、reminders、conversation、memory_tools、system_tools 与 MCP 解析代码。使用本机真实 libcjson.so.1（运行时报告 cJSON 1.7.18），头文件声明对照官方 1.7.18 ABI。该临时头文件和动态库不提交仓库；项目正常开发环境应使用已经解析的 managed cJSON 源码。没有把存储、JSON 或 MCP 逻辑替换成模型猜测；存储失败通过注入 Save 回调模拟，SD 恢复通过主机临时文件测试。

修改前，已按 Git blob SHA 核对所读取的原始文件，并运行原始 Note/Conversation 的定向基线测试：CRUD、容量、失败回滚、重启恢复、损坏数据拒绝、保存版本及旧保存完成通知隔离通过。

## 本次通过

| 检查 | 结果 |
|---|---|
| `tools/check_memory_contract.py` | **504 项运行时 CHECK 断言通过**，启用 ASan + UBSan。数字是断言次数，不是 504 个独立测试用例。 |
| `tools/check_system_tools.py` | 原系统行为回归通过；工具发现更新为 26 工具/9 页；严格字段、通知不写入、重试、普通笔记 CRUD/恢复/回滚/容量、专注到期、异步队列取消/过期、SD 双槽恢复。 |
| `tools/check_reminders_contract.py` | 原提醒行为回归通过；独立 6 工具/2 页、北京时间、重复/单次/离线到期、持久化稍后提醒、日期边界、恢复/回滚/容量。 |

记忆断言覆盖：新轮次开始前冻结上一轮；current/previous 来源区分；来源版本失效、取消和截断拒绝；原文不能由工具伪造或通过旧 save 覆盖；重复请求与连接缓存清空后的归档去重；schema 1→2、重启恢复；项目/关键词/处理状态筛选；乐观版本冲突；实际提醒关联、取消后的 missing、解除关联；保存失败不更新内存版本；UTF-8/NUL/字节/容量限制；八条上限长度记录；真实文件 CRC/双槽损坏保护；八线程同时归档只提交一次；并发编辑只允许一个版本成功；长内容分页末行可达。

分页测试执行实际 `Paginate` 算法，但使用固定字宽函数，不等同于真实字体/像素渲染测试。长换行案例的原文末尾仍能访问，只保留一页行数据。现有其他页面的 Wrap 未修改。

## 未执行，不应据此宣称通过

当前执行环境没有 ESP-IDF SDK、Xtensa 编译链或已连接设备。因此本次 **没有运行完整 `idf.py build`，没有生成已验证固件/发布镜像，没有烧录，没有人工语音→云端→设备闭环，没有完整 UI 预览/字库检查或真实屏幕光学观察，也没有 SD 拔卡/硬件断电、任务堆栈峰值或续航测量**。原有仓库里过去的真机记录不是本提交的实机证据。

在原开发环境部署前需要继续确认：完整构建及 CMake 集成、UI 契约与字体预览、新 26 工具的云端发现、两轮语音归档流程、无 SD/满卡/真实重启恢复、音频并发时归档堆栈余量。升级前备份 `/sdcard/notes/mcp.0` 与 `mcp.1`；schema 2 写入后旧固件不能直接读取，应使用升级前备份降级。

本次未改引脚、LUT/刷新波形、分区、联网凭据、提醒时间规则和录音长度限制；没有把普通应用更新变成全盘刷写。
