# 回合恢复、简约导航和天气：验证记录

日期：2026-09-23（北京时间；Actions 日志为 2026-09-22 UTC）。
基线：4b5d7cc22519592264df4c6fef6242422d3b7caf。
用户附件：WhiteAI-Xiaozhi-Turn-Stuck-20260923.zip。

## 证据和修复边界

附件记录 Transcribing、原文/回答为空，并明确说明翻译快捷操作没有真正触发。编码器启动/释放不能证明音频已到云端，STATE 的 busy 是屏幕刷新状态，不能据此断言没有后台任务。本次统一未完成回合的清理、超时和诊断，而不声称已证明服务端或麦克风是唯一根因。

旧代码已经有 45 秒等待超时，但部分清理路径会清除活动标志/计时器而不结束 Conversation。新版本在清理前结束未完成状态、保留已收到内容；等待 hello、等待识别、无有效进展、整轮上限分别有检查，避免情绪/心跳无限延长等待。成功完成的结果仍不受空闲断线影响；不自动重播任务或打开麦克风。

## 已执行的验证

隔离运行：https://github.com/446599/Metalio-WhiteAI/actions/runs/35774664970
输入 e5f627233983d2a8cc4472f2056ebb0090796efb；prepare、host、firmware、publish 完成。
发布后的普通源码：6afe073ea974c94e94819870238764900103d480。
默认分支直接采用如下已测试子树，不带入临时 .ci 文件及自动发布工作流：

| 子树 | Git tree SHA |
|---|---|
| main | 456b7a0d97e31648be09f9e05ee2cca169c3df3a |
| tools | f533be9d943b4dc97b55ddd04665507d0521473f |
| docs（追加本记录前） | 3616a92a461e0cfd6043a0d64a72e13707929841 |

已校验测试产物中的 38 份源文件 SHA-256 与本地测试版一致；完整本地主干子树在保留原串口脚本执行权限后与上述 main/tools/docs 完全一致。额外修改仅为本记录和常规只读 CI 中加入天气检查。默认分支二次运行以该交付提交的 Actions 为准。

| 检查 | 结果 |
|---|---|
| 21 组主机回归 | 全部通过；保留聊天、音频、睡眠、存储、MCP、串口及刷新检查 |
| 天气解析/配置队列/缓存 | 424 次 CHECK；UTF-8、地址转义、坐标、单位、时间、WMO 代码、错误数据、取消代次及并发快照 |
| 实际 DashboardService | 模拟 HTTP/NVS：有界读取和连接关闭、搜索、失败提交回滚、成功选城、重启缓存恢复、不同城市不混用缓存、无网错误 |
| 小智实际客户端逻辑 | 无响应20秒边界、提前STT后manual stop、情绪消息不延长进展、孤立识别状态、旧epoch、实际二进制发送计数/失败、hello和整轮上限 |
| Clang 补充检查 | weather、history、mono controls、sleep 均通过 |
| 原 MCP/记忆 | 543次CHECK，27工具/9页；没有新增天气写入或凭据读取AI工具 |
| 原输入/联网 | 3176次CHECK |
| 原控制栏/阅读 | 4406次CHECK；实验BLE mock单独4408次，不作为默认产品无线电验收 |
| 实际字体/页面 | 559帧：normal/offline/long/stale各112帧，empty111帧，另5张总览；缺字、越界、无框像素和天气编辑/取消/保存路由通过 |

CHECK 数量不是独立测试用例数。新天气与相关并发测试使用 ASan/UBSan；RTOS、设备网络和NVS按测试需要模拟。预览使用仓库真实字库及C++绘图，天气值是明确的测试数据，不是实测天气或屏幕照片。

## 外部天气服务探测：未通过，不计为集成成功

本轮另外尝试从 GitHub runner 请求 Open-Meteo Geocoding（Shanghai 作为公共测试地点，不是用户位置）再请求 Forecast。实际日志在第一次 Geocoding HTTPS TLS 握手时约15秒超时，没有产生 weather-live.json，也未取得天气响应。因此不能宣称已完成真实天气API连通性、完整端到端天气或用户当地网络测试。

该步骤是预先设置的可选网络探测（continue-on-error），GitHub 步骤列表可能显示 success；只有原始日志能区分它与成功获取数据。21组必需测试、实际页面预览和完整目标构建均不是可选步骤，失败会阻止发布。没有关闭TLS证书校验或把天气错误改成伪造数据以使测试通过。

文档核对来自 Open-Meteo 官方 Forecast、Geocoding 和条款页面。固件仍需在设备联网后实际搜索/选城并取得响应；请求失败保留已有缓存、显示失败。没有缓存时显示未配置/等待/失败，不显示假气温。

## 完整目标构建与内存

官方 espressif/idf:v6.0.1 容器运行真实 idf.py build，1405个Ninja步骤完成，最终ELF、镜像、分区范围与audio-memory-budget均通过。

| 项目 | CI结果 |
|---|---:|
| 应用大小 | 3209856字节（0x30fa80） |
| 5 MiB应用槽剩余 | 2033024字节，约39% |
| IRAM总占用 | 91392字节 |
| 共享内部静态占用 | 148912字节 |
| 相比4b5d7cc内部静态增量 | 288字节 |
| BLE禁止符号/预算错误 | 均为空 |
| 原上限IRAM/内部静态 | 97280 / 151552字节；没有放宽 |

本次CI应用SHA-256：ec5fb872012d89bdec3cd41a49b8abf938260c81b7a7d7ae301ed47ed09e8f4e。
字体与分区表哈希保持不变。重建镜像可能受构建时间等影响，使用当次delivery_manifest.json。

没有改动USB/上电、音频栈、BLE默认开关、锁屏睡眠、字库、分区或LUT；天气复用已有Dashboard任务，不增加常驻网络任务栈。静态内存预算通过不是运行时最大连续块或扬声器正常的实测保证。

## 部署与仍需验证

沿用ESP-IDF6.0.1，先build，核对实际串口与设备分区后仅app-flash；不清NVS/SD，不重刷字体或OTA选择。天气在设备设置→天气→选择城市配置；个人非商业公共端点不要求API key，商业部署遵循提供方条款。使用与隐私说明见TURN_WEATHER.md。

本轮没有刷写实体设备。需分别实测：真人语音/静默/网络中断后可重试、三个MCP快捷操作、实际城市搜索及天气刷新、离线缓存/重启/换城市、锁屏唤醒、TTS/铃声与最大连续堆。不能把上一版音频验收替代本版本验收。
