diff --git a/docs/TURN_WEATHER.md b/docs/TURN_WEATHER.md
new file mode 100644
index 0000000..52a0bd3
--- /dev/null
+++ b/docs/TURN_WEATHER.md
@@ -0,0 +1,47 @@
+# 回合收尾、简约页面与天气
+
+基线：4b5d7cc22519592264df4c6fef6242422d3b7caf。
+证据：用户的 WhiteAI-Xiaozhi-Turn-Stuck-20260923.zip；没有把之前的音频/USB实机验收当成本次验收。
+
+## 卡住的回合
+
+附件可确认多轮没有 STT 文本，最终 state=3、原文/回答为空；翻译快捷操作并未被真正触发。README 把编码器就绪解读为上行发送成功、把 STATE busy=0 解读为没有任务运行，这两个判断不能仅由这些日志成立：前者不能证明云端收到语音，后者是屏幕刷新状态。本次不据此排除麦克风、音频发送或云端问题。
+
+旧代码已有45秒等待超时，但释放连接、操作失效等分支会先清掉回合标志和计时器，未必同步结束 Conversation 状态。此次统一在清理前将未完成回合收尾为 Error、保存已收到内容；完成的回答不受空闲断线影响。
+
+- 等待应用 hello 10秒、松手等待识别20秒、无有效文本/音频进展45秒，以及整轮180秒的有界检查。轮询可能受当前网络操作影响，不能当成硬实时截止时间。
+- emotion/心跳不延长等待；提前到达的 STT 能被接收，并补发 manual stop，不把 Thinking 改回 Transcribing。
+- 实际 WebSocket 二进制写入成功后才计数；写入失败由网络任务关闭该回合，不从采集回调释放连接。正常松手后被过滤的帧不算传输错误。
+- 所有释放路径终止未完成状态；孤立的“正在识别”（没有活动回合和待处理操作）会收尾。旧连接迟到数据仍按 epoch 拒绝。
+- 重连只恢复连接，不自动录音或重放用户任务。元数据 reset 记录 cause/epoch/state/active/tx_packets/tx_bytes，不打印正文或凭据。
+
+三个快捷操作仍需要服务发现并读取 self.chat.get_task；本次没有用真实云端账号复现翻译，不承诺服务端一定返回回答。参见 CHAT_HISTORY.md。
+
+## 页面
+
+首页六个入口去掉外框；应用和设备设置去掉黑色选中底块与重复说明，笔记与网络列表取消装饰性选择框。触摸不留选中背景；实体键导航仍保留细短线，避免失去当前位置。输入框、键盘、确认按钮和铃声等真实状态控件不当成装饰框移除。纯1-bit，原字库、图标和首页热区不变。
+
+小智空闲页、胶囊、应用目录、笔记空态、阅读和控制栏删去重复操作教程。错误、截断、存储失败、未保存退出确认与密码安全行为保留；并非隐藏真实错误来获得简约界面。
+
+## 天气使用
+
+设备设置 → 天气 → 选择城市。输入中文、拼音或英文，选择带地区的结果；取得匹配地点后才存储配置，不默认把北京当作所在地，也不使用 IP 自动定位。保存失败保留原城市。
+
+接入 Open-Meteo 的 Geocoding 与 Forecast HTTPS API：温度、体感、湿度、风速和 WMO 天气现象。单位固定为摄氏度和 km/h，显示数据提供方的时间；离线/过期/刷新失败不清空上次有效结果。城市与天气缓存分别使用单个 NVS blob，缓存还绑定请求来源，切换城市不串用旧数据。原显式配置的 HTTPS weather_url 保留兼容；首次选择城市后优先使用城市配置。
+
+后台默认10分钟轮询（沿用 dashboard/refresh_minutes 设置），手动刷新最短间隔1分钟。休眠暂停网络请求，进行中的语音回合让天气任务延后；复用既有 dashboard 任务，不新增常驻任务栈。响应限制16KiB、HTTP超时10秒；搜索最多5个结果，有取消代次校验。具体等待还受原联网建立过程影响。
+
+来源和使用条件：
+- https://open-meteo.com/en/docs
+- https://open-meteo.com/en/docs/geocoding-api
+- https://open-meteo.com/en/terms
+
+面向个人非商业使用的公共端点不要求 API key；商业部署应遵循提供方条款。页面保留 Open-Meteo 署名。搜索词/选定坐标会发送给天气提供方，不发送 Wi-Fi 密码、小智 token 或聊天内容。网络必须能访问这两个服务；不能用主机/CI测试替代用户当地网络可达性与实际设备认证。返回的是模型天气数据，不标榜为本地传感器实测。
+
+## 验证和部署
+
+运行 check_ai_contract.py、check_weather_contract.py、原20组检查，以及 render_ui_preview.py；完整 ESP-IDF 6.0.1 构建和现有 audio-memory-budget 必须通过，不放宽预算。
+
+本次不更改音频栈、BLE默认关闭、USB/上电修复、分区、字体或屏幕波形。只在核对设备分区后 app-flash；不擦NVS/SD，不重写字体或OTA选择。
+
+上机分别验证：真人语音静默/断网收尾后可重试、提前STT及正常TTS；三项快捷操作真实MCP；城市搜索/取消/保存/重启缓存；离线天气及休眠后恢复；音频铃声、内部最大连续块和耗电。源码/模拟/构建通过不能保证这些物理结果。
diff --git a/docs/UI_QA_CONTRACT.md b/docs/UI_QA_CONTRACT.md
index 5df489f..216c553 100644
--- a/docs/UI_QA_CONTRACT.md
+++ b/docs/UI_QA_CONTRACT.md
@@ -69,3 +69,11 @@ POWER短按锁屏/唤醒，长按仍原关机流程。唤醒按下/松手/Click
 完整构建仍必须通过audio-memory-budget，检查最终ELF的IRAM/DRAM及不应链接的BLE符号，不能仅凭Flash余量验收。主机预览、串口CRC、编译和写入哈希不能替代实体触摸、残影、TTS、运行时最大连续内存、闹钟唤醒或电流测量；没有实机证据时必须明确报告未验证。
 
 使用、日志隐私和验收顺序见[SLEEP_CONNECTION.md](SLEEP_CONNECTION.md)；具体构建证据见[SLEEP_CONNECTION_VALIDATION_20260922.md](SLEEP_CONNECTION_VALIDATION_20260922.md)。
+
+## 2026-09-23：简约导航和天气
+
+覆盖前述导航装饰约定，不改变首页六项热区。首页不绘制任何卡片/选中框；触摸去掉焦点，实体 NEXT/PREV 保留短下划线而非方框。应用/设备设置仅图标、名称和细分隔线。设备设置仍5行，第3项改为“天气”（此前“刷新数据”）；天气页内保留刷新。首页“笔记”及原有录音的应用入口不变。
+
+天气页 y152城市，y220温度，y290现象，y374/428/482体感/湿度/风速，y548数据时间，y588联网/刷新状态。底部 y624、x32/248、200×48为选城市/刷新，y688为416×48返回。搜索结果5行，y184+84*i、76高；搜索页y624重新搜索。输入复用拼音键盘，90 UTF-8字节限制。天气不能在显示锁里执行HTTP/NVS；只提交带代次的有界命令。保存不可被中途取消，失败不更改旧城市，取消后旧搜索结果不得重新出现。
+
+预览使用实际字库和天气绘图/输入代码；注入的气温不当成真实天气。覆盖未配置、搜索中、候选、保存失败、有效结果和过期离线缓存。错误/截断/密码/未保存提示仍显示，静态教程不占据每一页底栏。
